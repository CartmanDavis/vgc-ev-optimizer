# EV Optimizer
This CLI tool will optimize an EV spread for a given pokemon for VGC. The input includes parameters of what to optimize for (offensive, defensive, and speed), and the script will then attempt to return viable EV spreads that satisfy the parameters. Eventually, this logic should be moved into a team builder :)
